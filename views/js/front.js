/**
 * Monero Payment Module - Front JS
 *
 * Minimal shell — all payment logic is inlined in payment_execution.tpl
 * inside a self-contained IIFE so the HMAC token never leaks to global scope.
 *
 * This file provides only the CSS asset registration hook target.
 */
